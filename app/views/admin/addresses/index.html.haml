%h1 Listing addresses

%table
  %tr
    %th First name
    %th Last name
    %th Address
    %th City
    %th State
    %th Zip code
    %th Phone number
    %th Alternative phone
    %th Default
    %th
    %th
    %th

  - @addresses.each do |address|
    %tr
      %td= address.first_name
      %td= address.last_name
      %td= address.address
      %td= address.city
      %td= address.state
      %td= address.zip_code
      %td= address.phone_number
      %td= address.alternative_phone
      %td= address.default
      %td= link_to 'Show', address
      %td= link_to 'Edit', edit_address_path(address)
      %td= link_to 'Destroy', address, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Address', new_address_path
